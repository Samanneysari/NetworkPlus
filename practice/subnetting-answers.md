<div dir="rtl" align="right">

# پاسخ کارگاه Subnetting

1. ۵ بیت Host، ۳۲ آدرس کل.
2. `255.255.248.0`.
3. `/29`.
4. `/26` با ۶۲ Host.
5. `/23` با ۵۱۰ Host.
6. ۶۲.
7. دو برابر: ۵۱۲ در برابر ۲۵۶ آدرس کل.
8. `/32`.
9. RFC 3021 دو آدرس را روی Point-to-point به Endpointها می‌دهد و Network/Broadcast سنتی لازم نیست.
10. خیر؛ `/28` فقط ۱۴ Host معمولی دارد.
11. N=`192.0.2.0`، B=`.15`، H=`.1–.14`، ۱۴.
12. N=`192.0.2.64`، B=`.127`، H=`.65–.126`، ۶۲.
13. N=`192.0.2.128`، B=`.255`، H=`.129–.254`، ۱۲۶.
14. N=`198.51.100.192`، B=`.223`، H=`.193–.222`، ۳۰.
15. N=`198.51.100.4`، B=`.7`، H=`.5–.6`، ۲؛ آدرس داده‌شده Broadcast است.
16. N=`203.0.113.200`، B=`.207`، H=`.201–.206`، ۶؛ آدرس داده‌شده Network است.
17. N=`203.0.113.0`، B=`.255`، H=`.1–.254`، ۲۵۴.
18. N=`10.10.32.0`، B=`10.10.47.255`، H=`10.10.32.1–10.10.47.254`، ۴۰۹۴.
19. N=`10.10.0.0`، B=`10.10.63.255`، H=`10.10.0.1–10.10.63.254`، ۱۶۳۸۲.
20. N=`10.20.128.0`، B=`10.20.255.255`، H=`10.20.128.1–10.20.255.254`، ۳۲۷۶۶.
21. N=`172.16.16.0`، B=`172.16.19.255`، H=`172.16.16.1–172.16.19.254`، ۱۰۲۲.
22. N=`172.20.248.0`، B=`172.20.255.255`، H=`172.20.248.1–172.20.255.254`، ۲۰۴۶.
23. N=`10.0.0.0`، B=`10.1.255.255`، H=`10.0.0.1–10.1.255.254`، ۱۳۱۰۷۰.
24. N=`198.51.100.64`، B=`.127`، H=`.65–.126`، ۶۲؛ آدرس داده‌شده Network است.
25. Network=`192.0.2.0`، Broadcast=`.31`؛ آدرس داده‌شده Broadcast و Host معتبر نیست.
26. `10.70.0.0/25`، `10.70.0.128/26`، `10.70.0.192/27`؛ از `.224` تا `.255` آزاد.
27. `10.71.0.0/24`، `10.71.1.0/25`، `10.71.1.128/26`، `10.71.1.192/30`.
28. چهار `/24`: `172.16.8.0`، `.9.0`، `.10.0`، `.11.0`.
29. هشت `/27` با گام ۳۲: `.0`، `.32`، `.64`، `.96`، `.128`، `.160`، `.192`، `.224`.
30. پنج `/27`؛ هرکدام ۳۰ Host. سه `/27` دیگر آزاد می‌ماند.
31. بله: `10.1.0.0/25`، `10.1.0.128/26` و `10.1.0.192/26`.
32. یک `/27` یا ۳۲ آدرس کل: `10.70.0.224/27`.
33. نیاز Rounded: ۱۰۲۴+۲۵۶+۱۲۸=۱۴۰۸ آدرس؛ کوچک‌ترین توان دو ۲۰۴۸، یعنی `/21`، با رعایت مرز.
34. تا شبکه کوچک فضای پیوسته لازم برای شبکه بزرگ را Fragment نکند.
35. خیر؛ اول `.0–.127` و دوم `.128–.191` است.
36. `10.90.0.0/22`.
37. `192.168.8.0/23`.
38. خیر؛ Summary مشترک شبکه‌های میانی/اضافی را نیز شامل می‌کند. Routeهای جدا دقیق‌اند.
39. A با `/24`، B را Remote و به Gateway می‌فرستد؛ B با `/16`، A را Local و ARP می‌کند. Asymmetric belief می‌تواند ارتباط را خراب کند.
40. Gateway بیرون Subnet محلی `/24` است؛ Client نمی‌تواند MAC Next hop محلی معتبر را Resolve کند. Gateway باید مثلاً `192.168.20.1` یا Mask/طرح اصلاح شود.

</div>
